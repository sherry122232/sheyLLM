1. ### 第 1 条

> Instruction fine-tuning (SFT) works best when we are just extracting pre-training behaviors, not adding new ones

**核心道理**：  
SFT 最好的用途是“**把模型已经学会的东西引导出来**”，而不是“教它全新的知识或能力”。

**通俗例子**：

- 一个高中生已经学完了高中数学（预训练阶段）。
    
- 你让他做高考题（SFT），只是帮他学会“按题目格式写出解题步骤”。
    
- 如果你让他学大学微积分里根本没见过的概念，那就是在“添加新行为”，SFT 效率很低，且容易学坏。
    

**实际启示**：

- 不要在 SFT 里硬塞模型预训练时没见过的**事实或逻辑**。
    
- SFT 是做“格式、风格、指令跟随、输出结构”的引导，不是做知识注入。
    

---

### 第 2 条

> Adding (factually correct!) data can sometimes hurt

**核心道理**：  
即使是**正确**的数据，加进去也可能**伤害**模型表现。

**通俗例子**：  
你教孩子写作文。

- 正确句子：“今天天气很好，所以我去了公园。”
    
- 如果你硬加 1 万条这样的句子，虽然都对，但孩子只会写这种简单结构，反而不会写复杂论述、抒情、议论文了。
    

**为什么会 hurt？**

- **分布偏移**：你加的数据类型单一，会压低模型原有的多样能力。
    
- **过拟合**：模型记住这些正确例子，但在没见过的情况表现变差。
    
- **干扰通用格式**：比如强行把问答都变成“先复制问题再回答”，在不需要复制的问题上也会出错。
    

**实际启示**：

- 不是“数据越多越好”。
    
- 数据的**多样性**比“正确性”在 SFT 里更关键。
    

---

### 第 3 条

> Small amounts of the right kinds of behavior (safety, instruction-following, style) make a big difference, but there is a long-tail that benefits from more data

**核心道理**：

- 少量高质量数据能显著改善“安全、指令遵循、风格”这类基础行为。
    
- 但模型的**长尾能力**（少见但有用的请求）需要更多数据才能覆盖。
    

**通俗例子**：

- 用 **200 条**精心设计的对话数据，就能让模型学会礼貌、不生成有害内容、按格式输出 ✅
    
- 但如果用户问：“把这段 Python 代码转成 Rust，并用西班牙语注释”，这种组合很少见，需要大量长尾数据才能让模型遇到并学会。
    

**实际启示**：

- **先做小规模高质量 SFT**（几百到几千条），解决 80% 的核心行为。
    
- **再针对长尾任务**（罕见用例、特定格式、小众语言等）持续增加数据。

# rlhf

sft is expensive 
